services:
  - type: web
    name: django-f1score-app
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: gunicorn mi_proyecto.wsgi
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: mi_proyecto.settings
